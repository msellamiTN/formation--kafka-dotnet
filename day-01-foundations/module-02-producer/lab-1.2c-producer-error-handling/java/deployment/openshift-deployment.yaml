apiVersion: apps/v1
kind: Deployment
metadata:
  name: ebanking-producer-resilient-java
  labels:
    app: ebanking-producer-resilient-java
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ebanking-producer-resilient-java
  template:
    metadata:
      labels:
        app: ebanking-producer-resilient-java
    spec:
      containers:
        - name: api
          image: ebanking-producer-resilient-java:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
              name: http
          env:
            - name: SERVER_PORT
              value: "8080"
            - name: KAFKA_BOOTSTRAP_SERVERS
              value: "kafka-svc:9092"
            - name: KAFKA_TOPIC
              value: "banking.transactions"
            - name: KAFKA_DLQ_TOPIC
              value: "banking.transactions.dlq"
            - name: MAX_RETRIES
              value: "3"
            - name: RETRY_BACKOFF_MS
              value: "1000"
            - name: CIRCUIT_BREAKER_THRESHOLD
              value: "5"
            - name: CIRCUIT_BREAKER_OPEN_MS
              value: "60000"
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: ebanking-producer-resilient-java
  labels:
    app: ebanking-producer-resilient-java
spec:
  selector:
    app: ebanking-producer-resilient-java
  ports:
    - name: http
      port: 8080
      targetPort: 8080
---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: ebanking-producer-resilient-java
spec:
  to:
    kind: Service
    name: ebanking-producer-resilient-java
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
