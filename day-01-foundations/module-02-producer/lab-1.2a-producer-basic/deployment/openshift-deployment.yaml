# OpenShift Deployment for E-Banking Producer API (Lab 1.2a)
# Usage: oc apply -f deployment/openshift-deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ebanking-producer-api
  labels:
    app: ebanking-producer-api
    module: producer
    lab: "1.2a"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ebanking-producer-api
  template:
    metadata:
      labels:
        app: ebanking-producer-api
    spec:
      containers:
        - name: api
          image: image-registry.openshift-image-registry.svc:5000/${NAMESPACE}/ebanking-producer-api:latest
          ports:
            - containerPort: 8080
          env:
            - name: Kafka__BootstrapServers
              value: "kafka-svc:9092"
            - name: Kafka__Topic
              value: "banking.transactions"
            - name: ASPNETCORE_ENVIRONMENT
              value: "Development"
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "500m"
          readinessProbe:
            httpGet:
              path: /api/Transactions/health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 15
          livenessProbe:
            httpGet:
              path: /api/Transactions/health
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: ebanking-producer-api
  labels:
    app: ebanking-producer-api
spec:
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: ebanking-producer-api
---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: ebanking-producer-api
spec:
  to:
    kind: Service
    name: ebanking-producer-api
  port:
    targetPort: 8080
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
